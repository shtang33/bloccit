<h1>New Summary</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for summaries:</p>
     <ul>
       <li>Make sure the summary is appropriate.</li>
       <li>Never insult dogs.</li>
       <li>Smile when you type.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @summary do |s| %>
       <div class="form-group">
         <%= s.label :name %>
         <%= s.text_field :name, class: 'form-control', placeholder: "Enter summary name" %>
       </div>
       <div class="form-group">
         <%= s.label :body%>
         <%= s.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter summary" %>
       </div>
       <%= f.submit "Save", class: 'btn btn-success' %>
     <% end %>
   </div>
 </div>